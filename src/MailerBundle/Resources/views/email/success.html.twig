{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col s10 offset-s2">
            <h3>Your email have been send sucessfull.</h3>
            <div class="col s8 offset-s4">
                <a class="waves-effect deep-purple btn" href="{{ path('email') }}">
                    <span class="deep-purple">Send more messages</span>
                </a>
            </div>
        </div>
    </div>
{% endblock %}